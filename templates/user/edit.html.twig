{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1 class="text-center mt-2 mb-5">Fiche de {{ user.nom }} {{ user.prenom }}</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Valider'}) }}

    <a href="{{ path('app_user_index_personnel') }}">
        <button class="btn btn-dark">Liste du personnel</button>
    </a>
    <a href="{{ path('app_user_index_eleve') }}">
        <button class="btn btn-dark">Liste des elèves</button>
    </a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
